# Homebrew Tap For AgentSetup

Install AgentSetup with:

```bash
brew install wenqiangde/agentsetup/agentsetup
```

This repository only stores the Homebrew formula. The AgentSetup CLI source and
release workflow live in:

```text
https://github.com/wenqiangde/agent-setup
```
